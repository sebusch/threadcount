repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Pin to a specific release; bump intentionally when upgrading ruff.
    rev: v0.9.10
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
